import React, { useEffect } from "react";

const MapSearchList = (props) => {

  useEffect(()=>{
  })
  
  return <div className="map-list-item">{props.item.address_name}</div>;
};

export default MapSearchList;
